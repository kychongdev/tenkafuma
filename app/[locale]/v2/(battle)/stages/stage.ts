import { GameState } from "../GameState";

export function stage(gameState: GameState) {
  const stage = gameState.stage;
  switch (stage) {
    case "s21_63": {
      break;
    }
    case "r21_sp": {
      break;
    }
    case "dummy": {
      break;
    }
  }
}
