//export function checkTaunt(gameState: GameState, position: number) {
//  if (position >= 20 && position < 25) {
//    return gameState.enemies[position - 20].buff
//      .map((buff) => {
//        switch (buff.taunt) {
//          case true: {
//            return buff;
//          }
//          default:
//            return;
//        }
//      })
//      .filter((x) => x);
//  }
//}
