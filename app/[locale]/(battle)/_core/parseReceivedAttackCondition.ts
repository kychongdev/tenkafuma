import { Condition } from "@/app/[locale]/(battle)/_types/Skill";
import { GameState } from "./GameState";
import { triggerSkill } from "./triggerSkill";
export function parseReceivedAttackCondition(
  position: number,
  condition: Condition[],
  state: GameState,
  oldState: GameState,
) {
}
